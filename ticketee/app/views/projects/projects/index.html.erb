<h2 class='text-4xl my-4'>Projects</h2>

<%= link_to "New Project", new_projects_project_path, class: "btn btn-blue mb-4" %>

<ul class="list-inside list-disc">
  <% @projects.each do |project| %>
    <li><%= link_to project.name, project %></li>
  <% end %>
</ul>
